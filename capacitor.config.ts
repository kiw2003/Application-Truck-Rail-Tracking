import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Thai Rail Parcel Tracking',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
